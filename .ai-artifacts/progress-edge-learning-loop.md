# Progress Log — edge-learning-loop

Spec: make the "edge" (knowledge-boundary object) a first-class, tool-enforced artifact; merge workshop+recall into one learning skill; edges in summary-page frontmatter, generated into meta/edges.md.

Decisions locked with PM (2026-06-24):
- Edge-as-shared-object framing approved
- Merge wiki-workshop + recall into one skill (keep name wiki-workshop)
- Edges in summary-page frontmatter; meta/edges.md generated by rebuild pipeline
- Platform persisted as `## Bridge` section on summary page (hybrid option A + generated index)

## Log

- 2026-06-24-exploration-Mapped extension architecture: indexer.ts rebuilds registry/backlinks/index; integration.ts is thin (no content checks); lint.ts has per-type frontmatter requirements; summary template has Integration targets + Open questions sections; activity.ts computes lifecycle backlog (integratedAwaitingRecall); agent_end rebuilds generated artifacts after writes.
- 2026-06-24-extension-Implemented edge object end-to-end: types.ts (Edge/EdgeRecord/EdgesData, edges on RegistryEntry, LintRun.counts.edges, LifecycleBacklog.openEdges); indexer.ts (parseEdges with gray-matter Date coercion, collectEdges, renderEdgesMarkdown, rebuild writes meta/edges.json+edges.md); scaffold.ts summary template gains `edges: []` + `## Bridge`; integration.ts blocks integration when summary lacks edges/Bridge/concrete targets; lint.ts mode `edges`; activity.ts surfaces openEdges; index.ts lint enum + status output + integrate tool description.
- 2026-06-24-decision-Edges NOT added to SUMMARY_REQUIRED frontmatter lint: would error on every existing vault page; dedicated `edges` lint check with warning severity chosen instead (upgrade-friendly).
- 2026-06-24-skills-Merged recall into wiki-workshop as graduation mode (new instructions/graduation.md); updated SKILL.md, protocol.md (Phase 3 persists Bridge, Phase 5 writes edges), platform.md (platform no longer disposable), rules.md (graduation rules), checklist.md; deleted skills/recall; updated wiki-intel protocols/output-format (frontier section), brain-wiki page-lifecycle wording, README (graduation row + skill list).
- 2026-06-24-tests-Added edges.test.ts (9 tests: parse/collect/render, lint edges mode, integrate enforcement). Fixed capture-state.test.ts fixture to carry learning artifacts. 34/34 pass in affected suites; 3 failures in context-guards/project-sync/context-gather-agent confirmed pre-existing via git stash on clean checkout.
- 2026-06-24-gotcha-gray-matter parses unquoted YAML dates into Date objects — parseEdges must coerce Date → ISO string or edge ages silently compute as 0.
- 2026-06-24-gotcha-DEFAULT_SUMMARY_TEMPLATE and scaffold docs are TS template literals — backticks in markdown must be escaped (\`) or the file fails to parse.
- 2026-06-24-verify-npm run check passes; smoke-tested rebuild → meta/edges.md renders frontier groups; openEdges flows into scanActivity lifecycle backlog.
- 2026-06-24-deferred-Full `bun test extensions/brain-wiki/src/` hangs when all files run together (passes per-file; likely socket/network fixture interaction) — pre-existing, not investigated.
